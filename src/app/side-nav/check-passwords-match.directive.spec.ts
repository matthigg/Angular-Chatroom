import { CheckPasswordsMatchDirective } from './check-passwords-match.directive';

describe('CheckPasswordsMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckPasswordsMatchDirective();
    expect(directive).toBeTruthy();
  });
});
